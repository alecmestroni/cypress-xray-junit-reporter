{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open",
    "test": "npx cypress run",
    "moreTests": "cypress run",
    "requiredJiraKey": "cypress run --config-file requiredJiraKey.config.js",
    "single": "cypress run -s cypress/e2e/beforeHooks.cy.js",
    "fail": "cypress run -s cypress/e2e/fail.cy.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cypress-multi-reporters": "^1.6.3",
    "cypress-xray-junit-reporter": "file:.."
  },
  "devDependencies": {
    "cypress": "^15.4.0"
  }
}
